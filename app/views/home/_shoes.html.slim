.home-shoes-container
  span.count #{ @shoes.count } matching products
  span.sort
  .shoes
    - shoes.each do |shoe|
      .col.s4
        .item
          .image
            = image_tag(shoe.image)
          p.title = shoe.name
          p.code = shoe.shoe_code
          p.price $#{shoe.price}
          .card
            a class="waves-effect waves-light btn"
              i class="fa fa-shopping-cart" 
              | Add to card